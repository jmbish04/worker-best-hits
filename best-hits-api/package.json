{
  "name": "best-hits-api",
  "private": true,
  "version": "0.1.0",
  "description": "Modular Cloudflare Worker monorepo with frontend, backend, and shared agentic packages.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "migrate": "cd apps/worker && wrangler d1 migrations apply DB --remote",
    "deploy": "turbo run build --filter=@cloudflare/frontend && pnpm run migrate && cd apps/worker && wrangler deploy",
    "deploy:dry-run": "pnpm run build",
    "build-types": "cd apps/worker && wrangler types",
    "frontend:dev": "cd apps/frontend && npm run dev",
    "frontend:build": "cd apps/frontend && npm run build",
    "frontend:preview": "cd apps/frontend && npm run preview"
  },
  "devDependencies": {
    "turbo": "^1.13.0",
    "wrangler": "^4.44.0"
  }
}
